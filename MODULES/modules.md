# what are modules?
set of instruction which is already wrriten and dont need write it again , just simply use it
# types
- in-built-no need to install
- external- should install from package manager
# pip
```bash
pip install MODULE_NAME
```
# what is pip
its kind of package manager which will install package from internet

